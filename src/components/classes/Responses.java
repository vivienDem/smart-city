package components.classes;

import components.interfaces.ResponseI;
/**
 * <code>Responses</code> enumere les reponses possibles suite a une action
 *
 * @author Adan Bougherara et Vivien Demeulenaere
 */
public enum Responses implements ResponseI{
	SUCCESS, FAILURE
}
